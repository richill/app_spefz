<%# render 'form' %>

<% provide(:title, "Update Hosts") %>
<div class="eventhosts_edit_background">
  <div class="eventhosts_inner_container">
    <div class="header">Update Event Attendance</div>
    <div class="content_list">
      <div class="event_title">Event: <%= link_to @event.title, event_path(@event) %></div>
      <%= render 'hosts/form' %>
    </div> 
  </div>
</div>
