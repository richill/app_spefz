<div class="users_upcomingevents_section">
  <div class="users_upcomingevents_inner_section">
    <div class="event_container">
      <% Event.all.each do |event| %>
      <ul>
        <li>
        <%= event.title %>
        <%= event.description %>
        <%= event.address %>
        <%= event.postcode %>
        </li>
      </ul>
      <% end %>
    </div>
  </div>
</div>