<div class="report_modal_container">
  <div id="<%= dom_id(social) %>_report" class="reveal-modal report_modal" data-reveal aria-labelledby="modalTitle" aria-hidden="true" role="dialog">
    <h2 id="myModal-report-header">Report this social | <%= social.title %></h2>
    <div class="content_info"><%= render partial: "reports/form_social", locals: { social: social } %></div>
    <a class="close-reveal-modal cancel_button" aria-label="Close">&#215;</a>
  </div>
</div> 