<div class="invite_user_section">
  <div id="<%= dom_id(@user) %>_inviterequest" class="reveal-modal invite_user_modal" data-reveal aria-labelledby="modalTitle" aria-hidden="true" role="dialog">
    <h2 id="modalTitle-invite" class="header">invite request</h2>
    <div class="invite_form"><%= render partial: "invite_requests/form", locals: { inviter: @social.user, user: current_user } %></div>
    <a class="close-reveal-modal cancel_button" aria-label="Close">&#215;</a>
  </div>
</div>