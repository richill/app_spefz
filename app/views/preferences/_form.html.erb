<%= simple_form_for(@preference) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :town %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
