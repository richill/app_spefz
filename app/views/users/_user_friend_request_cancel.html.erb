<%# form_for(current_user.active_friendship_requests.find_by(friended_id: @user.id), html: { method: :delete }, remote: true) do |f| %>
  <%# f.submit "Request Sent", class: "btn btn_friend_request_cancel_submit" %>
<%# end %>


<%= link_to 'Cancel Request', friendship_path(user_id: current_user, friend_id: @user), controller: "friendships", action: "cancel", method: :delete, class: "btn btn_friend_request_cancel_submit" %>