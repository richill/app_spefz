<h1>total photos [<%= @photos.count %>]
  <div class="h1_subtitle"><i class="fa fa-info-circle"></i> displays all photos on the platform [ordered by created date]</div>
</h1>

<div id="">
  <div class="bottom_inner_content">
    <div class="table">
      <div class="tablerow">
        <div class="tablecell header background">photos</div>
        <div class="tablecell background"></div>
        <div class="tablecell background"></div>
        <div class="tablecell background"></div>
        <div class="tablecell background"></div>
        <div class="tablecell background"></div>
        <% if current_user.admin_pa_management_group || current_user.pa_administration_group %>
          <div class="tablecell background"></div>
        <% end %>
      </div>
      <% @photos.order("created_at desc").each do |photo| %>
        <div class="tablerow">
          <div class="tablecell" style="width:10%">
            <div class="image_container">
              <div class="image">
                <% if photo.album_image_link.present? %>
                  <%= link_to image_tag(photo.album_image_link), photo, target: "_blank" %>
                <% else %>
                  <%= link_to image_tag(photo.image_url), photo, target: "_blank" %>
                <% end %>
              </div>
            </div>
          </div>
          <div class="tablecell">
            <div class="user_name"><%= link_to (truncate "#{photo.name}", length: 30), photo, target: "_blank" %></div>
            <span class="faint_text_format">created: <%= photo.created_at.strftime("%a, %d %b %Y") %></span>
          </div>
          <div class="tablecell">
            <div class="faint_text_format">published date</div> 
            <div class="main_text_format"><%= photo.date.strftime("%a, %d %b %Y") %></div>
          </div>

          <div class="tablecell">
            <div class="faint_text_format">photos</div> 
            <% if photo.photos.present? %>
              <div class="main_text_format"><%= link_to "#{photo.photos.count}", "#" %></div>
            <% else %>
              <div><%= "--" %></div>
            <% end %>
          </div>
          <div class="tablecell">
            <div class="faint_text_format">created by</div> 
            <div class="main_text_format"><%= link_to "spefz team", "#" %></div>
          </div>
          <div class="tablecell" style="width:25%;">
            <div class="main_text_format report_content_view">
              <%= (truncate "#{album.description}", length: 100) %>
              <a href="#<%= album.id %>" data-reveal-id="<%= dom_id(album) %>_album_content_show" style="">view</a>
              <%= render partial: 'shared/modal_album_content', locals: { album: album } %>
            </div>
          </div>
          <% if current_user.admin_pa_management_group || current_user.pa_administration_group %>
            <div class="tablecell">
              <div class="delete_btn"><%= link_to 'Edit', edit_album_path(album) %></div>
              <div class="delete_btn"><%= link_to 'Destroy', album, method: :delete, data: { confirm: 'Are you sure?' } %></div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>

<div class="clear"></div>